{% extends 'base.html.twig' %}

{% block title %}Hello ProfileController!{% endblock %}

{% block content %}

<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ app.user.getFullName() }}! ✅</h1>

    {% for success in app.session.flashbag.get('success') %}
    <p class="success">{{ success }}</p>
        
    {% endfor %}

    <p>Bienvenue sur ton profil, tu peux accéder à tes informations depuis cette page, pour gérer tes commandes, modifier tes données ou bien supprimer ton compte.</p>

    <hr>

    <p>Nom : {{ app.user.getLastName }}</p>
    <p>Prénom : {{ app.user.getFirstName }}</p>
    <p>Email : {{ app.user.getEmail }}</p>
    <p>Mot de passe : {{ app.user.getPassword }}</p>


    <hr>


    <p>Change tes informations <a href="{{ path('app_edit_profile') }}">ici</a></p>
    <p>Change de mot de passe <a href="">ici</a></p>
    <p>Gère tes commandes <a href="{{ path('app_order') }}">ici</a></p>
    <p><a href="{{ path('app_logout') }}">Déconnexion</a></p>

</div>
{% endblock %}
